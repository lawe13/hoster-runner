name: self hosted runners

on:
  workflow_dispatch:

jobs:
  testing:
  runs-on: self-hosted
  steps:
  - name: testing runner
    run: |
         echo "Kewal running self hosted actions runner"
         echo "Okay!!"
      